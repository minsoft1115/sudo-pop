# Installed by `sudo-pop --init`.
#
# Aliases only expand in interactive shells, so scripts, `sh -c`, Makefiles and
# systemd units keep using the real sudo. Use `\sudo` to bypass this on purpose.
alias sudo='sudo-pop'
